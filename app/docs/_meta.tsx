import type { MetaRecord } from 'nextra'

/** Sidebar cho nhánh `/docs/*` — thứ tự & tiêu đề hiển thị */
const meta: MetaRecord = {
  index: 'Tổng quan',
  'getting-started': 'Bắt đầu',
  configuration: 'Cấu hình',
}

export default meta
